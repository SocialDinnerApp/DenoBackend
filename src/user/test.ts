

// import {register, login, logout, validateToken} from "./api.ts";
import { delay } from "https://deno.land/std@0.121.0/async/delay.ts";
import { assertEquals } from "https://deno.land/std@0.97.0/testing/asserts.ts";



// Test if username exists and redirect via button to register tab
Deno.test("Fail if username does not exist #fail", async () => {
    
  
  });
  
  
  
  
  // Test if all mandatory fields are filled
  Deno.test("Fail if not all mandatory fields are filled #fail", async () => {
    
  });
